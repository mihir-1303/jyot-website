import Link from "next/link";
import type { PublicCollectionSummary } from "../lib/types/public";
import { EditorialImage } from "./EditorialImage";

export function CollectionSection({ collection, title }: { collection: PublicCollectionSummary; title: string }) { return <section className="section border-t divider"><div className="page-shell"><div className="mb-6 flex items-end gap-3 border-b pb-2.5 divider"><h2 className="serif text-2xl tracking-tight md:text-3xl">{title}</h2><span className="mb-1.5 h-1.5 w-1.5 shrink-0 bg-[var(--orange)]" aria-hidden="true" /></div><div className="grid gap-6 md:grid-cols-[minmax(0,1fr)_minmax(16rem,28rem)] md:items-center"><Link className="card-link" href={`/collections/${collection.slug}`}>{collection.coverImage ? <EditorialImage src={collection.coverImage} alt={collection.title} /> : <div className="editorial-thumbnail" aria-hidden="true" />}</Link><div><p className="eyebrow">Curated series · {collection.itemCount} stories</p><h3 className="serif mt-2 text-4xl leading-none md:text-5xl"><Link href={`/collections/${collection.slug}`}>{collection.title}</Link></h3><p className="mt-4 leading-7 text-[var(--muted)]">{collection.description}</p><Link className="publication-close-action mt-6" href={`/collections/${collection.slug}`}>View series <span className="arrow">→</span></Link></div></div></div></section>; }
