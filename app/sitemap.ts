import type { MetadataRoute } from "next";
import { getPublishedArticles, getPublishedCollections, getPublishedVideos, getPublishedResearch } from "../lib/data/public";
import { canonical } from "../lib/seo";
export default async function sitemap(): Promise<MetadataRoute.Sitemap> { const base = [{ url: canonical("/"), changeFrequency: "daily" as const, priority: 1 }, ...["/articles", "/videos", "/research", "/collections"].map((url) => ({ url: canonical(url), changeFrequency: "daily" as const, priority: .7 }))]; try { const [articles, videos, research, collections] = await Promise.all([getPublishedArticles(), getPublishedVideos(), getPublishedResearch(), getPublishedCollections()]); return [...base, ...collections.map((item) => ({ url: canonical(`/collections/${item.slug}`), lastModified: item.updatedAt, changeFrequency: "weekly" as const, priority: .7 })), ...articles.map((item) => ({ url: canonical(`/articles/${item.slug}`), lastModified: item.updatedAt, changeFrequency: "weekly" as const, priority: .7 })), ...videos.map((item) => ({ url: canonical(`/videos/${item.slug}`), lastModified: item.updatedAt, changeFrequency: "weekly" as const, priority: .7 })), ...research.map((item) => ({ url: canonical(`/research/${item.slug}`), lastModified: item.updatedAt, changeFrequency: "weekly" as const, priority: .7 }))]; } catch { return base; } }
